
=========
TODO List
=========

Major
=====

1. Burn rate computation and graph

Minor
=====

1  Running twistd -b afloat (debug mode) breaks process protocol and causes
   google api calls to fail

2  show future debits from savings / clean up defaultAccount stuff

3  fix caching of javascript and images from static.. max-age=0??

4  Hyperlink from events to the google calendar entry, so you can edit them.
   Instances of recurring events should have an extra HL to edit the original

5  speed up fixup step by doing all fixups in one request

6  scheduler table sometimes gets off by a few pixels and floats down to the
   next line
